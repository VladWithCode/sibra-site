package properties

import "github.com/vladwithcode/sibra-site/internal/db"

templ ForYou(props []*db.Property) {
    <div class="for-you-section">
        for _, prop := range props {
            <div class="property-item">
                <h3>{ prop.Address }</h3>
                <p>{ prop.Price }</p>
            </div>
        }
    </div>
}
